apiVersion: v1
kind: Service
metadata:
  name: mosque-clock-api
  namespace: mosque-clock-ai
  labels:
    app: mosque-clock-api
spec:
  type: ClusterIP
  selector:
    app: mosque-clock-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
